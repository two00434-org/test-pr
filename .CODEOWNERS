# This CODEOWNERS file is to define individuals or teams that are responsible for code in a repository. Code owners are automatically requested for review when someone opens a pull request that modifies code that they own
# Syntax: Each line is a file pattern followed by one or more owners.

# The metadata_table file need PR review from one of two reviewer
/files/metadata_table.csv zhaihotmail
